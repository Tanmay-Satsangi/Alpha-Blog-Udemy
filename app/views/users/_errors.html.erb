<!-- This file is created by the udemy tutorials as a recommendation. -->

<% if obj.errors.any? %>
  <h2> The following errors prevented the <%= obj.class.name.downcase %> from being saved </h2>

  <ul>
    <% obj.errors.full_messages.each do |msg| %>
        <% puts msg%>
        <li><%= msg %></li>
    <% end %>
  </ul>

<% end %>
